import BorderMagic from '@app/BorderMagic';
import { AutoMath, ComponentRenderWithoutError } from '../testHelper';

describe('BorderMagic', () => {
  describe('BorderMagic rendering', () => {
    it('work', () => {
      expect(false).toBeFalsy();
    });
  });
  describe('BorderMagic Interactions', () => {});
  describe('BorderMagic Interactions', () => {});
});
