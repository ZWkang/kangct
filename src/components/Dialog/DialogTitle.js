import React from 'react';
import styled from 'styled-components';
import { H6 } from '../Heading/index';

const DialogTitle = styled(H6)`
  padding: 5px 10px;
  border-top-left-radius: 6px;
  border-top-right-radius: 6px;
  margin: 0;
`;

export default DialogTitle;
